public with sharing class Simple {

    // Trigger a redeploy

    public Static Decimal SimpleInterest(Integer P, Decimal T, Decimal R) {

        return (P * T * R) / 100;


        //TESTING IT

    }
}